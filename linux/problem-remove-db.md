## Не удаляется База Данных из psql:
Бывает, что БД существует, что проверить в psql можно ```\l```. Но при попытке ```DROP DATABASE name;``` ошибка, что База Данных не найдена. Можно решить проблему:
``` bash
sudo su postgres
dropdb -i -e name
```